# CV directory

Place the production CV PDF here as `santiago-rivera-cv.pdf`.

The Download CV button reads its path from `src/content/site.ts → contact.cv`.
While the file is missing, the value is flagged `pending: true` and a small
"pending" badge is rendered next to every download CTA.
