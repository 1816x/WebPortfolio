# Portrait directory

Drop the headshot here as `santiago.jpg` (recommended 4:5 ratio, ≥ 1600px on the
long edge).

The intro section reads the path from `src/content/site.ts → person.portrait`.
A placeholder is rendered until the value is unmarked from `pending: true`.
