/**
 * The react-three-fiber signature field was removed in the neo-brutalist
 * redesign: it crashed at desktop width (React reconciler mismatch) and the
 * new hero uses a lightweight DOM card-stack instead. This stub stays only so
 * any lingering import resolves; it renders nothing.
 */
export function SignatureCanvas() {
  return null;
}
